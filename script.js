function calculate() {
    let input1 = 0.5;
    let input2 = 0.8;
    
    let Weight1 = 0.6;
    let Weight2 = 0.2;
    let bias = -2;

    let weightedSum = (input1 * Weight1) + (input2 * Weight2) + bias;
    console.log(weightedSum);

}